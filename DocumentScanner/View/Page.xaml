<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DocumentScanner.View.Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DocumentScanner.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:CommunityToolkit.WinUI.Converters"
    mc:Ignorable="d">

    <Grid BorderBrush="Black" Background="White" BorderThickness="1" Margin="0,10,0,0">
        <Grid.Resources>
            <converter:BoolToVisibilityConverter x:Key="BoolToVisiblityConv" TrueValue="Visible" FalseValue="Collapsed"/>
        </Grid.Resources>
        <Image Source="{Binding PageSource}" Margin="20,5,20,5"/>
        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="15" FontSize="16" Text="{Binding PageNumber}"/>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{Binding Color}"/>
        <Button HorizontalAlignment="Stretch" Background="{Binding Color}" VerticalAlignment="Stretch" Command="{Binding SelectedPage}" Visibility="{Binding Benabled, Converter={StaticResource BoolToVisiblityConv}}"/>
    </Grid>
    
</UserControl>
